#menu
  = link_to t("links.menu.freebies"), freebies_path, :class => (params[:controller] == "freebies" ? "tab selected" : "tab")
  = link_to t("links.menu.resellers"), resellers_path, :class => (params[:controller] == "resellers" || params[:controller] == "purchase_expectations" ? "tab selected" : "tab")
  = link_to t("links.menu.documents"), documents_path, :class => (params[:controller] == "general" && params[:action] == "documents" ? "tab selected" : "tab")
  = link_to t("links.menu.contact"), contact_path, :class => (params[:controller] == "general" && params[:action] == "contact" ? "tab selected" : "tab")
  .clear 
