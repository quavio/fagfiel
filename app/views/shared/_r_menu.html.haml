#menu
  = link_to t("links.menu.freebies"), freebies_path, :class => (params[:controller] == "freebies" ? "tab selected" : "tab")
  = link_to t("links.menu.seasonal_purchase_expectations"), reseller_seasonal_purchase_expectations_path(current_user.reseller.id, Date.today.year, Date.today.month), :class => (params[:controller] == "seasonal_purchase_expectations" ? "tab selected" : "tab")
  = link_to t("links.menu.contact"), contact_path, :class => (params[:controller] == "general" && params[:action] == "contact" ? "tab selected" : "tab")
  = link_to "Montagem de rolamentos", "/montagem_de_rolamentos.pdf", :title => "Download das perguntas mais frequentes sobre montagem de rolamentos", :target => "_blank"
  .clear 
